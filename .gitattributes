# 셸 스크립트는 LF 유지. CRLF 로 바뀌면 bash 가 '\r' 을 명령 일부로 읽어
# push_code.sh 등이 깨진다(젯슨=리눅스에서도 실행되므로 특히 중요).
*.sh text eol=lf
run.sh text eol=lf

# 텍스트 설정 파일도 LF 로 통일(젯슨과 노트북이 같은 파일을 공유)
*.yaml text eol=lf
*.yml text eol=lf
*.json text eol=lf
*.jsonl text eol=lf
