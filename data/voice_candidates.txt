# F2 미세조정 후보 (2026-08-19 3라운드)
#
# 2라운드 블라인드 결과: **순정 F2 가 이겼다.** 다만 그대로 쓰기엔 밋밋하다는 평.
# 그래서 여기서는 **F2 를 기준으로 조금씩만** 움직인다. 넓게 훑는 단계는 끝났고
# 이제는 좁게 파는 단계다(탐색 -> 활용).
#
# 프리셋 성격 (공식):
#   F1 차분·약간 낮은 톤 / F2 밝고 명랑·장난기·youthful / F3 아나운서·방송용
#   F4 선명·자신감·표현력 / F5 친절·부드러움·차분
#   M2 깊고 묵직          / M4 부드럽고 중성적, **youthful·friendly**
#                            ^ 여성 프리셋 밖에서 유일하게 'youthful' 이 붙는다.
#                              F2 에 조금 섞으면 성별색이 옅어지며 아이 쪽으로 간다.
#
# 섞는 양을 0.15 와 0.25 두 단계로 뒀다. 0.15 가 아예 티가 안 나면 그것도 정보다
# (스타일 공간에서 이 정도 움직임은 안 들린다는 뜻 -> 다음엔 더 크게 가면 된다).
#
# 돌리는 법 (젯슨):
#   python tools/voice_knockout.py --specs-file data/voice_candidates.txt --k 2
#
# 🔴 여기서 목소리를 정한 **다음에** 시드와 속도를 잡는다. 둘은 조합 문자열로
#    표현이 안 되는 축이라 이 목록에 못 넣는다:
#      python tools/voice_tournament.py --stage 4 --from "<정한 값>" --blind   # 시드
#      python tools/voice_tournament.py --stage 5 --from "<정한 값>" --blind   # 속도
#    ⚠️ **시드가 '뭔가 좀 그런' 느낌의 범인일 수 있다.** 같은 스타일인데 캐릭터가
#       달라진다. 지금 777 은 F1 기준으로 고른 값이라 F2 에 맞는다는 보장이 없다.
#
# 🔴 현행 목소리(F1+F4)는 도구가 몰래 끼워 넣는다(총 14명).
# ⚠️ 가중치 합은 전부 1.0.

# ── 대조군 ────────────────────────────────────────────────────────────────────
F2                             # 2라운드 우승. 또 이기면 순정이 답이다

# ── 조금 섞기 (0.15) ──────────────────────────────────────────────────────────
F2:0.85+F1:0.15                # 살짝 차분하게
F2:0.85+F4:0.15                # 살짝 또렷하게
F2:0.85+F5:0.15                # 살짝 부드럽게
F2:0.85+M4:0.15                # 살짝 중성적으로 (M4 = youthful·friendly)

# ── 조금 더 섞기 (0.25) ───────────────────────────────────────────────────────
F2:0.75+F5:0.25
F2:0.75+M4:0.25

# ── 조금 빼기 (외삽) ──────────────────────────────────────────────────────────
F1:-0.15+F2:1.15               # 차분함을 뺀다 = 더 밝게
F3:-0.15+F2:1.15               # 방송 아나운서 느낌을 뺀다
M2:-0.15+F2:1.15               # 성인 저음을 뺀다

# ── 음색은 F2 그대로, 말투만 ──────────────────────────────────────────────────
# '목소리는 좋은데 말이 촐랑거린다' 싶으면 여기서 잡힌다. 2세에겐 이게 크다.
F2|F1                          # 말투만 차분하게
F2|F5                          # 말투만 부드럽게
F2:0.85+F5:0.15|F1             # 음색 살짝 부드럽게 + 말투는 차분하게
