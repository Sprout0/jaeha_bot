# 모델 평가 원본 로그

`tools/eval_llm.py` 가 남긴 실행 기록. **답변 전문이 들어 있다.**

`logs/` 는 git 미추적이라 여기 있던 파일들이 각 기계에만 남아 있었다(2026-08-11 에 옮김).
논문·보고의 근거 자료이고, 판정 규칙이 바뀔 때마다 다시 채점해야 해서 추적 대상으로 둔다.

## 다시 채점하는 법

판정기(`app/safety.py`·`app/claims.py`)를 고쳐도 **API 재호출 없이** 옛 실행을 다시 채점할 수 있다:

```bash
python tools/eval_llm.py --rescore reports/eval/eval_20260807_163703.json
```

문항 수가 다른 실행끼리 비교할 땐 `--ids-from` 으로 문항을 맞춘다.

## 들어 있는 것 (2026-08-07)

프롬프트 강화 전후의 안전·날조 평가. 이날 얻은 결론은
[[jaeha-bot-progress]] 의 '미해결' 항목에 정리돼 있다 — 요지는 **날조와 안전 실패의 원인이
모델이 아니라 프롬프트였다**는 것이다(API 모델 4종이 똑같이 실패했고, 규칙을 넣자 14/28 → 0/28).

- `eval_20260807_163703.json` — 가장 큼(78KB). 여러 모델 비교
- `eval_20260807_18*.json` — 젯슨 로컬 EXAONE 기준선(안전 14문항×2회)

⚠️ 평가셋은 전부 **가상의 유아 발화**다. 실제 아이 녹취는 API 로 보내지 않는다.
