# === 재하봇 1 의존성 ===
# ⚠️ 젯슨은 이 파일로 설치하지 않는다. conda env `jaeha_bot`(py3.10) + 젯슨 전용 pip
#    인덱스(https://pypi.jetson-ai-lab.io/jp6/cu126)를 쓰고, 일부는 소스빌드다
#    (ctranslate2 CUDA, llama-cpp-python CUDA). 아래 목록은 **PC 개발 기준**이다.
#    젯슨 설치 절차와 함정은 메모리 jaeha-bot-progress 참조.

# --- 음성 STT/TTS ---
faster-whisper>=1.0.0
# ⚠️ 젯슨은 faster-whisper 를 --no-deps 로 깔고 ctranslate2 는 CUDA 소스빌드본을 쓴다
#    (인덱스 휠에 CUDA 빌드가 없어 그냥 깔면 CPU 로 떨어진다).
# TTS: Supertonic-3 (한국어, ONNX Runtime 구동, OpenRAIL-M). torch 불필요, 경량.
# 모델 가중치는 첫 실행 시 HF(Supertone/supertonic-3)에서 자동 다운로드(~수백MB).
# (Piper 는 공식 한국어 없음 + 커뮤니티 KSS 발음 누락, MeloTTS 는 기계음이라 미채택)
supertonic>=1.3.0
sounddevice>=0.4.6
soundfile>=0.12.1
# 젯슨 ReSpeaker 는 16000Hz 전용인데 Supertonic 합성은 44100 → 재생 전 리샘플이 필요하다.
# 없으면 선형보간으로 폴백하지만 음질이 떨어진다. (app/tts_module.py:_resample)
soxr>=0.3.7

# --- ONNX 런타임 (호출어 KWS + TTS 가속) ---
# app/wake_onnx.py 와 app/tts_module.py 가 직접 import 한다. 빠지면 호출어가 안 뜬다.
# 젯슨은 GPU 판(onnxruntime-gpu 1.24.0)이 이미 깔려 있으므로 여기 것을 덮어쓰지 말 것.
onnxruntime>=1.17.0

# --- LLM (로컬, 양자화 GGUF) ---
# 0.3.21+ 의 py3-none PC용 wheel 은 AVX-512 로 빌드돼 있어 AVX-512 미지원 CPU
# (예: Intel Core Ultra / Lunar Lake)에서 로드 시 Illegal Instruction(0xc000001d)
# 로 죽는다. 그래서 CPU 전용 wheel 이 따로 제공되는 0.3.2 로 고정.
# PC 설치: pip install "llama-cpp-python==0.3.2" \
#            --extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cpu
# ⚠️ 젯슨은 0.3.34 CUDA 소스빌드. 인덱스 prebuilt 0.3.14 는 생성 시 무조건 크래시한다.
llama-cpp-python==0.3.2

# --- 공통 ---
numpy>=1.24
pyyaml>=6.0
pydantic>=2.5

# --- 모델 비교 평가용 (tools/eval_llm.py 전용, 런타임 아님) ---
# 젯슨 실행에는 필요 없다. 평가를 돌리는 기계(노트북)에만 있으면 된다.
openai>=2.0.0
google-genai>=2.0.0
python-dotenv>=1.0.0      # .env 에서 API 키 읽기(앱 재시작 불필요)

# --- 비전 (STEP 9, 미구현) ---
# app/vision_module.py 가 아직 스텁이라 **어떤 코드도 import 하지 않는다.**
# 셋 다 무거우니 비전을 실제로 붙일 때 주석을 풀 것.
# ultralytics>=8.1.0        # YOLOv8
# mediapipe>=0.10.9
# opencv-python>=4.9.0

# --- PC 개발용에서만 (Jetson 베이스 이미지엔 이미 포함) ---
# torch>=2.2
# torchvision>=0.17
